#include <stdio.h> 
#include <math.h> 
int main() 
{ 
    double p=2300; 
    double rate=7;
    double time=4; 
    double amount = p*((pow((1 + rate / 100), time))); 
    double CI = amount-principal; 
    printf("CI: %lf", CI); 
    return 0; 
}
